import { Table as TableBody, TableRow, TableCell } from './table';

const Table = {
    Body: TableBody,
    Row: TableRow,
    Cell: TableCell
};
export default Table;
export { TableBody, TableRow, TableCell };

